<br>
<div class="form-group">
    <label for="{{$id}}">{{$label}} <span class="text-danger">*</span></label>
    <input class="form-control border border-success" type="{{$type}}" name="{{$name}}" id="{{$id}}" placeholder="{{$placeholder}}" value="{{$value}}" autocomplete="off"/>
</div>
